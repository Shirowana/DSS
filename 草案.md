# 草案



你现在要帮助我实现一个新的 PEFT 方法，暂时仍命名为 DSS，并且代码目录直接命名为 `dss`。

注意：这不是复现旧 DSS，也不是复现 SpIEL / PiCa / LoCA 中的任何一个方法；而是一个新方法。你需要先充分理解参考材料，再严格按我下面定义的新方法来设计和实现。

====================
一、任务目标
====================

我们要实现一个新的 PEFT 方法，方法名暂定为 DSS。

它的核心思想是：

1. 先对同功能组的 target modules 做 shared-basis 预处理
   - 对每个功能组（先考虑 q_proj / k_proj / v_proj / up_proj / down_proj）收集对应层的预训练权重矩阵
   - 用 `diagonalization.py` 这类经验损失去求一组共享变换 A, B
   - 训练阶段固定 A, B，不参与更新
   - 对每层来说，可以把权重增量写成：
     ΔW = A^{-1} · ΔΛ · B^{-1}
   - 我们真正训练的是稀疏的 ΔΛ，而不是 dense 全量参数

2. 在 Λ / core 空间里做动态 sparse selection
   - 搜索空间先按“全矩阵”理解，不只限制在对角带内
   - 每层维护：
     - elite pool：真正参与训练和前向的稀疏 ΔΛ slots
     - candidate pool：只做 probe / 统计梯度，不参与参数更新
   - 第一阶段：先通过候选→晋升机制，把 elite pool 填满
   - 第二阶段：进入慢频率的 prune / grow
     - prune：优先丢弃长期更新很小的 elite 参数
     - grow：从当前 candidate pool 中选择高分候选补进来
     - grow 来源不是随机直接进 elite，而是候选池中的高分项
   - 第二阶段不再把 pinball quantile 当作核心决策器，而更偏向 score 排序 + top-k + margin gate

3. 借鉴来源
   - 借鉴 SpIEL：slot-based 稀疏参数管理、动态 prune/grow、必要时更新 optimizer state
   - 借鉴 PiCa：同功能层共享 basis / shared structure 的思想，但不是直接复现 PiCa
   - 借鉴 LoCA：如何把一个非标准参数化方法写成规范的 HuggingFace PEFT 结构（config / layer / model / register）
   - 借鉴旧 DSS：candidate / elite 的两区语义、pinball quantile 在第一阶段候选筛选中的作用
   - 但再次强调：最终实现的是“我们这个新 DSS”，不是任何单篇论文的代码复刻

====================
二、这次第一轮你要做什么
====================

先不要急着写所有代码。

第一轮你的目标是：

1. 认真阅读我提供的材料
2. 输出一份清晰的 implementation plan
3. 明确第一版最小可实现版本（v0）应该包含哪些文件
4. 先只考虑以下 PEFT 核心文件：
   - `__init__.py`
   - `config.py`
   - `layer.py`
   - `model.py`
5. 你可以额外建议是否需要：
   - `shared_basis.py`
   - `selection_utils.py`
   - `selector.py` / `trainer.py`
   但必须说明：
   - 为什么需要
   - 第一版是否必须实现
6. 暂时不要实现：
   - quantization / bnb / gptq 兼容
   - 自定义 optimizer
   - 全套 trainer 改造
   - 复杂的 merge/unmerge 导出逻辑
   - 旧 DSS 的外循环 reset
7. 第一版目标不是完整论文复现，而是：
   - 先做出一个结构规范、接口清晰、符合 PEFT 风格的最小版本
   - 能说明参数放在哪里、forward 怎么走、哪些状态由 layer 持有、哪些状态应交给 selector / trainer

====================
三、目录与风格要求
====================

1. 目录直接命名为 `dss`
2. 如果仓库里已经有旧的 `dss`，请你先审慎比较，不要盲目覆盖
3. 优先复用 HuggingFace PEFT 的既有风格
4. 代码风格要求：
   - 模块职责清晰
   - 不把太多逻辑全塞进 layer.py
   - 给出必要注释
   - 先追求结构正确，再追求花哨优化
5. 对于“第一阶段候选晋升”和“第二阶段 prune/grow”，你可以先把接口设计出来，必要时把第二阶段先留 TODO，但要把状态组织方式定清楚

====================
四、你必须先回答的问题
====================

在动手写代码前，请你先输出以下内容：

1. 你如何理解这个新 DSS，与旧 DSS / SpIEL / PiCa / LoCA 的区别分别是什么
2. 你建议的 v0 文件树
3. 每个文件的职责边界
4. 你认为第一版是否必须有：
   - shared_basis.py
   - selection_utils.py
   - selector.py / trainer.py
5. 你建议 layer 内部维护哪些状态
6. 你建议哪些状态放到 model / selector 层
7. 第一版 forward 应该先支持到什么程度
8. 哪些部分第一版先不做，只留扩展接口

在你回答完这些问题，并给出计划后，我再决定让你进入具体实现。