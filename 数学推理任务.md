# 数学推理任务

这份文档用于固定 DSS 项目的数学推理实验主协议，作为后续实现、训练、评测和论文写作的统一参考。

## 1. 最终实验设置

```text
Models:
  LLaMA3-8B
  Qwen2.5-7B

Train:
  math_10k.json

Eval:
  GSM8K
  SVAMP
  AQuA
  MAWPS

Baselines:
  LoRA
  DoRA
  SMT
  DSS
```

这个设置是合理的。它接近 MISA / LoRaDA 一类 2025 年 PEFT 论文使用的四任务 Math10K 协议：在 LLaMA3-8B 和 Qwen2.5-7B 上，用 MATH10K 训练，并评测 GSM8K、SVAMP、AQuA、MAWPS。

这样做的好处是：避开 AddSub / MultiArith / SingleEq 三个和 MAWPS 来源样本存在重叠风险的旧评测项。根据 LLM-Adapters 官方说明，`math_10k.json` 来自 GSM8K、MAWPS、AQuA，其中 MAWPS 又包含 AddSub、MultiArith、SingleEq，因此训练 `math_10k.json` 后不应再把这三个作为 evaluation benchmark，而应改为直接评测 MAWPS test set。

## 2. 训练轮数

主实验统一使用：

```text
epochs = 3
```

理由：

- 这一系列 Math10K 设置中，`3 epochs` 是最常见也最稳的选择。
- LLM-Adapters 官方 Math10K 示例命令使用 `num_epochs=3`。
- MISA 的 math reasoning 超参数表中，LoRA / DoRA 在 LLaMA3-8B 和 Qwen2.5-7B 上也使用 `epochs=3`。

建议论文中统一写法：

```text
Following recent Math10K-based PEFT settings, we fine-tune all methods for 3 epochs.
```

除非专门做 ablation，否则不建议随意改成 1 epoch 或 5 epoch。

## 3. 公平比较原则

超参数不能随便设，但也不要求机械复刻某一篇论文。更重要的原则是：同一张实验表里，所有方法必须公平。

```text
same model
same train data
same eval data
same epochs
same max length
same batch size
same prompt template
same decoding
same answer extraction
same LR search rule
```

## 4. 推荐初始超参数

建议以 MISA 的 LLaMA3-8B / Qwen2.5-7B Math 设置作为 LoRA / DoRA 的初始参考：

```text
LoRA / DoRA:
  rank = 32
  alpha = 32
  dropout = 0.05
  optimizer = AdamW
  epochs = 3
  batch size = 4 × 1
  warmup steps = 0
  target modules = Wq, Wv, Wup, Wdown
```

推荐初始学习率：

```text
LLaMA3-8B:
  LoRA lr = 2e-4
  DoRA lr = 2e-4

Qwen2.5-7B:
  LoRA lr = 1e-4
  DoRA lr = 1e-4
```

对于 DSS 和 SMT，不建议直接复制 LoRA 的学习率，而建议使用统一的小范围搜索。结合 DSS 当前稀疏更新的特点，暂定：

```text
lr ∈ {5e-5, 1e-4, 1.5e-4, 2e-4}
```

其中 DSS 默认第一条启动实验建议使用：

```text
start_lr = 1e-4
```

推荐论文写法：

```text
For each method, we search the learning rate from {5e-5, 1e-4, 1.5e-4, 2e-4} and report the best validation setting.
```

这比只对 DSS 单独调参、更有可比性。

## 5. 最大长度设置

主实验建议统一：

```text
max_length / cutoff_len = 512
max_prompt_length = 256
```

理由：

- 数学任务往往包含 chain-of-thought，`256` 容易截断。
- 早期 LLM-Adapters 命令使用 `cutoff_len=256`，但那是更早期的小模型设置。
- 对 LLaMA3-8B / Qwen2.5-7B 这类当前主模型，`512` 更稳。

如果显存不足，可以统一退到 `256`，但不能不同方法用不同长度。

这里要特别区分：

- `max_length=512` 是总长度上限
- `max_prompt_length=256` 是 prompt 的单独上限

若总长度超过 `512`，后续脚本按以下顺序处理：

1. 先将 prompt 截到 `256`
2. 如果仍超过 `512`，再截 response
3. 记录是否发生截断，但第一版不因为截断直接过滤样本

## 6. 数据组织规范

数学任务当前采用两类数据目录：训练数据单独预处理，评测数据直接读取 LLM-Adapters/MISA-compatible 的本地 `test.json`。

```text
/root/datasets/math_reasoning/processed/
/root/datasets/math_reasoning/metadata/
/root/datasets/evaluate/
```

含义如下：

- `/root/datasets/math_reasoning/processed/`
  - 训练用数据
  - 已完成 tokenize，可直接被训练脚本读取
- `/root/datasets/math_reasoning/metadata/`
  - 记录 Math10K 训练数据来源、条数、哈希、截断统计
  - 用于核对而非训练/推理
- `/root/datasets/evaluate/`
  - 评测用 JSON 数据
  - 直接采用 LLM-Adapters 仓库的 `dataset/<task>/test.json`
  - evaluator 从这里读取 `instruction/input/output/answer` 字段并组装 prompt

### 6.1 当前评测数据口径

当前主实验不再单独生成 `raw_eval/`，也不再保留 `prepare_math_eval.py` 这类评测预处理脚本。评测数据直接从 LLM-Adapters 下载并放在：

```text
/root/datasets/evaluate/gsm8k/test.json
/root/datasets/evaluate/AQuA/test.json
/root/datasets/evaluate/SVAMP/test.json
/root/datasets/evaluate/mawps/test.json
```

数量应为：

```text
GSM8K = 1319
AQuA = 254
SVAMP = 1000
MAWPS = 238
```

这对应 LLM-Adapters/MISA 使用的 Math10K 评测协议，而不是逐个 Hugging Face 官方 split。特别是 `SVAMP=1000` 是 LLM-Adapters/MISA 口径，不是 HF 官方 `test=300`。

### 6.2 数据来源表述

论文和实验记录中建议写：

```text
We follow the Math10K evaluation protocol used by LLM-Adapters and MISA.
```

不要写成：

```text
official Hugging Face test splits
```

## 7. 训练模板与输出格式

后续数学任务训练模板整体参考 `diablo`，而不是完全放任原始数据集各自的输出风格混用。

### 7.1 统一训练模板

训练 prompt 统一采用 `diablo` 风格的 instruction 模板：

```text
Below is an instruction that describes a task. Write a response that appropriately completes the request.

### Instruction:
...

### Input:
...

### Response: Let's think step by step.
```

如果样本没有 `input` 字段，则只保留 `Instruction` 和 `Response` 块。

训练和评测均使用同一套 prompt，避免训练阶段和评测阶段出现额外任务说明不一致的问题。

### 7.2 输出风格

对所有数学推理样本，训练目标统一鼓励模型：

- 先 step by step
- 再给最终答案

训练数据使用 `math_10k.json` 中的原始 `output` 字段，不额外追加新的最终答案句子。也就是说，预处理脚本不会强制补：

```text
The final answer is: 42
```

原因是 Math10K 的原始输出中已经经常包含类似 `the answer is ...`、`The answer in Arabic numerals is ...` 等总结句。额外追加统一后缀可能造成模型学习到重复总结，例如：

```text
... The answer is 42.

The final answer is: 42
```

第一版先保持原始输出风格，只统一 prompt，不强行重写 response。

### 7.3 AQuA 约定

AQuA 仍然使用同一套 instruction 模板。训练时不单独重写为 `Answer: C` 或 `The final answer is: C`，而是保留 Math10K 原始输出。

评测时仍然按选择题逻辑处理：从模型新生成文本中抽取最后出现的 `A/B/C/D/E`，而不是抽数值。

- 四个数学任务使用同一套 prompt
- AQuA 只在 answer extraction 阶段特殊处理
- 减少训练模板分叉，同时避免人工追加答案句造成重复总结

## 8. 训练输出格式要求

评测最关键的是：raw output 可以有推理过程，但最终答案必须可解析。

### 8.1 GSM8K / SVAMP / MAWPS

模型输出可以是：

```text
We need to compute ...
...
The answer is 42.
```

评测脚本会优先匹配常见答案提示词，如果没有显式答案提示词，则回退到抽取最后一个数值。

### 8.2 AQuA

AQuA 是选择题，最终输出应能稳定落成选项字母：

```text
The answer is C.
```

评测脚本应抽取最后出现的 `A/B/C/D/E`，而不是数值。

### 8.3 截断策略

训练预处理时总长度采用：

```text
prompt + response + eos <= 512
```

如果样本超过长度限制，处理顺序为：

1. prompt 最多保留 `256` tokens
2. 如果仍超过 `512`，再截断 response
3. 记录是否发生截断
4. 强制保留 eos

即使不额外追加 `The final answer is: ...`，训练阶段仍然需要截断。原因是每个 batch 内的 token 序列必须有有限长度，否则显存、训练速度和不同方法之间的公平性都会失控。

评测时不使用这个训练截断规则；评测由 `max_new_tokens=512` 控制新生成长度。

## 9. 训练、验证与评测流程

后续数学推理任务统一采用：

1. 先对 `math_10k.json` 做 tokenize 预处理
2. 用训练脚本读取 `processed/` 中的训练数据
3. 用统一 evaluator 直接读取 `/root/datasets/evaluate/` 中的四个 benchmark `test.json`
4. 保存逐样本评测结果与总体准确率

### 9.1 训练集与验证集

训练集主来源：

```text
math_10k.json
```

验证集从 `math_10k.json` 中固定切分：

```text
validation_samples = 500
seed = 42
```

这样做的原因是：

- 用于学习率选择
- 用于 checkpoint 选择
- 避免直接拿测试集调参

与现有仓库对比：

- `MISA`：切 `500` 条 validation
- `SMT`：训练框架支持切 validation，但 math 公开脚本不完整
- `diablo`：不切 validation，直接训练后测试

当前 DSS 采用比 `diablo` 更稳的方案：固定切 `500` 条 validation。

### 9.2 训练时长与步数

目前已固定：

- `epochs = 3`

后续 `save_steps` 和 `eval_steps` 暂不写死，等确认 `math_10k` 的总 step 数后再定。

## 10. 评测脚本要求

代码中建议强制以下三点：

```text
1. 只解析模型新生成的部分，不解析 prompt。
2. GSM8K / SVAMP / MAWPS 抽最后一个数值答案。
3. AQuA 抽最后出现的选项字母 A / B / C / D / E。
```

### 10.1 只 decode 新生成 token

这一点比 `LLM-Adapters / MISA / diablo` 直接 `decode` 全串再按 `### Response:` 切更稳。

注意：

- 这是评测阶段的输出处理规则
- 与训练 loss 无关

训练时 loss 仍然是标准 causal LM token-level loss：

- 输入是 `prompt + response`
- prompt 部分 labels 设为 `-100`
- response 部分逐 token 计算交叉熵

## 11. 实验资产管理协议

`commonsense` 当前最值得复用的不是某个单独脚本，而是完整的实验资产管理闭环。后续 `math` 任务必须沿用同样的思路，保证每次实验都有：

- 训练日志
- 模型输出目录
- 评测结果目录
- 结构化 `record.json`
- 自动汇总 `runs.csv`

### 11.1 数学任务目录协议

后续 `math` 任务统一使用以下平行目录：

```text
logs_math/
output_math/
results_math/
experiments/math/
```

对应含义：

- `logs_math/`
  - 放训练日志、评测日志
- `output_math/`
  - 放每次训练产出的 adapter / checkpoint
  - 同时放每次 run 的 `record.json`
- `results_math/`
  - 放四个 benchmark 的逐样本预测 json 和 `summary.json`
- `experiments/math/`
  - 放 `runs.csv`
  - 放 `index.csv`
  - 如有需要，后续也可放每次实验的 md 记录

### 11.2 与 commonsense 主线的对应关系

当前 `commonsense` 的完整链路是：

1. 预处理脚本生成训练数据
2. 训练入口负责保存模型与日志
3. 独立 evaluator 负责逐任务评测
4. `update_run_record.py` 回填 `record.json`
5. `export_runs_csv.py` 汇总成 `runs.csv`

后续 `math` 必须照搬这套逻辑，只替换：

- 任务名
- 数据路径
- 评测任务列表
- 结果字段

## 12. 当前脚本清单

为了把 `commonsense` 这套流程平移到 `math`，当前保留以下主线脚本。

### 12.1 数据准备层

1. `data_processing/process_math10k.py`
   - 读取 `math_10k.json`
   - 统一 prompt 模板
   - 分开 tokenize prompt / response
   - 切出 `validation_samples=500`
   - 保存训练用 processed 数据

评测数据不再单独写预处理脚本。四个 `test.json` 直接来自 LLM-Adapters：

```text
/root/datasets/evaluate/gsm8k/test.json
/root/datasets/evaluate/AQuA/test.json
/root/datasets/evaluate/SVAMP/test.json
/root/datasets/evaluate/mawps/test.json
```

### 12.2 训练层

2. `finetune_math.py`
   - 对应 `finetune_commonsense.py`
   - 负责 DSS 在 `math_10k` 上训练
   - 负责 validation
   - 负责 checkpoint 输出

3. `run_dss_math.sh`
   - 对应 `run_dss.sh`
   - 只训练，不自动评测
   - 适合调参和单次训练

4. `train_eval_math.sh`
   - 对应 `train_eval.sh`
   - 训练完成后自动评测四个 math benchmark
   - 自动更新 `record.json`
   - 自动导出 `runs.csv`

### 12.3 评测层

5. `evaluate_math_reasoning.py`
   - 对应 `evaluate_commonsense.py`
   - 支持 `gsm8k / svamp / aqua / mawps`
   - 只 decode 新生成 token
   - 数值题抽最后一个数
   - AQuA 抽最后一个字母
   - 输出逐样本 json 和 `summary.json`

6. `scripts/eval_math_reasoning.sh`
   - 对应 `scripts/eval_commonsense.sh`
   - 循环跑四个 math 数据集
   - 写评测日志

### 12.4 记录与汇总层

7. `scripts/update_math_run_record.py`
   - 对应 `scripts/update_run_record.py`
   - 回填训练信息与评测结果到 `output_math/<run_name>/record.json`

8. `scripts/export_math_runs_csv.py`
   - 对应 `scripts/export_runs_csv.py`
   - 扫描所有 `record.json`
   - 导出到 `experiments/math/runs.csv`

## 13. record.json 与 runs.csv 规范

### 13.1 每次训练 run 的最小记录字段

每个 `math` run 在 `output_math/<run_name>/record.json` 中建议记录：

```text
run_name
code_version
training
evals
notes
```

其中 `training` 至少包含：

```text
model_name
method
target_modules
lr
batch_size_per_gpu
num_gpus
global_batch_size
num_epochs
max_length
max_prompt_length
val_set_size
eval_steps
save_steps
precision
seed
run_time
train_loss_final
eval_loss_best
best_step
```

其中 `evals` 至少包含：

```text
gsm8k
svamp
aqua
mawps
```

### 13.2 runs.csv 建议字段

`experiments/math/runs.csv` 第一版建议至少包含：

```text
run_name
code_version
model_name
method
target_modules
lr
batch_size_per_gpu
num_gpus
global_batch_size
num_epochs
max_length
max_prompt_length
val_set_size
eval_steps
save_steps
precision
seed
run_time
train_loss_final
eval_loss_best
best_step
gsm8k_acc
svamp_acc
aqua_acc
mawps_acc
notes
```

后续如需要，可再增加：

- `rank`
- `alpha`
- `dropout`
- `n_frequency`
- `candidate_size`
- `grad_store_steps`

## 14. Shell 配置默认值

后续 `math` 线的 shell 入口默认沿用 `commonsense` 的风格，并统一约定以下路径：

```bash
REMOTE_PROJECT_ROOT=/root/code/DSS
REMOTE_PEFT_SRC=/root/code/quest/peft/src
REMOTE_DATA_ROOT=/root/datasets
REMOTE_MODEL_ROOT=/root/hf_cache_models/models

LOG_ROOT=${REMOTE_PROJECT_ROOT}/logs_math
OUTPUT_ROOT=${REMOTE_PROJECT_ROOT}/output_math
RESULT_ROOT=${REMOTE_PROJECT_ROOT}/results_math
EXPERIMENT_ROOT=${REMOTE_PROJECT_ROOT}/experiments/math
```

### 14.1 训练相关默认值

```bash
MODEL_NAME=LLaMA3-8B
MAX_LENGTH=512
MAX_PROMPT_LENGTH=256
VAL_SET_SIZE=500
LR=1e-4
NUM_EPOCHS=3
BATCH_SIZE=8
GRAD_ACCUM_STEPS=2
NUM_GPUS=2
PRECISION=bf16
SEED=42
EVAL_STEPS=500
SAVE_STEPS=500
```

### 14.2 评测相关默认值

```bash
EVAL_BATCH_SIZE=1
EVAL_MAX_NEW_TOKENS=512
EVAL_NUM_BEAMS=1
EVAL_DO_SAMPLE=0
EVAL_TEMPERATURE=0.0
EVAL_MAX_EXAMPLES=0
```

### 14.3 DSS 相关默认值

如果后续直接跑 DSS 数学实验，shell 里还应统一暴露：

```bash
TARGET_MODULES=qkvud
N_FREQUENCY=180000
CANDIDATE_SIZE=30000
GRAD_STORE_STEPS=3
LOW=500
UP=4000
RATIO=0.1
THRESHOLD_MODE=oracle
SCORE_METHOD=snr
DSS_DROPOUT=0.05
```

当前默认参数量相关设置暂时不变，先只把目标模块扩展为 `qkvud`。

## 15. 推荐实现顺序

为避免后续反复重构，建议按以下顺序实现：

1. 先定目录协议
2. 先写 `process_math10k.py`
3. 再确认 `/root/datasets/evaluate` 中四个 LLM-Adapters `test.json` 数量和字段
4. 再写 `evaluate_math_reasoning.py`
5. 再写 `update_math_run_record.py`
6. 最后接 `run_dss_math.sh` 和 `train_eval_math.sh`

原因：

- `math` 线最容易反复变动的是数据口径和评测器
- 只要 evaluator 和 record schema 先定死，后面的训练壳子反而最好补

### 10.2 数值答案解析

第一版先照搬 `diablo` 的朴素数值解析规则：

- 去逗号
- 用正则找所有数字
- 取最后一个数字

为了兼容常见格式，仍可做最小 normalize：

```text
"1,000" -> 1000
"1000." -> 1000
"$1000" -> 1000
"1000 dollars" -> 1000
```

若最终转成数值，则允许极小误差：

```text
abs(pred - gold) < 1e-3
```

第一版暂不引入更复杂的 fraction/boxed parser。

### 10.3 AQuA 解析

AQuA 的解析规则固定为：

- 取最后出现的 `A/B/C/D/E`

这里明确学习 `MISA` 的做法，而不是 `LLM-Adapters` 取第一个字母的旧逻辑。

## 11. Decode 参数

为保证公平、可复现，并减少“解码策略收益”掩盖方法差异，主实验 decode 参数统一为：

```text
max_new_tokens = 512
do_sample = False
num_beams = 1
temperature = 0.0
```

说明：

- `MISA` 使用更老的模板：`temperature=0.1, top_p=0.75, top_k=40, num_beams=4`
- `diablo` math eval 也使用类似配置，并对不同任务使用 `256/512` 的不同 `max_new_tokens`
- `SMT` 公开 eval 更偏 deterministic，但常见是 `num_beams=4`

当前 DSS 为主实验统一选择：

- `num_beams=1`

原因：

- 更可复现
- 更省时间
- 更少把 beam search 的收益混进方法比较

如有需要，可在附录增加 `beam=1 vs beam=4` 对比，但主实验不使用 `beam=4`。

## 12. 结果保存与日志规范

### 12.1 结果文件

第一版不追求复杂 schema，直接采用与 `MISA / SMT / diablo` 风格接近的最小结果字段：

```text
question
gold_answer
output_pred
pred
flag
task_name
```

即：

- 每个样本都保存到结果 json
- 便于后续排查错误样本
- 但不做过度复杂的字段设计

### 12.2 终端日志

终端日志沿用当前 DSS 风格：

- 每个任务只打印前 `10` 条样本的详细输出
- 后续只打印进度和准确率汇总

也就是说：

- 完整预测结果写入 json 文件
- 终端不打印所有样本

## 13. 与已有仓库的对齐说明

### 13.1 LLM-Adapters

- 继承其总体范式：`instruction tuning + answer extraction`
- 不直接照搬其旧的 `AQuA` 第一个字母解析
- 不直接照搬其 `decode` 全串再切字符串的实现

### 13.2 MISA

- 学习其四任务协议：`AQuA / MAWPS / GSM8K / SVAMP`
- 学习其 `AQuA` 取最后一个字母的解析思路
- 不直接采用其 `num_beams=4` 和旧式生成参数

### 13.3 SMT

- 学习其训练数据兼容层思路：兼容 `instruction/input/output/answer`
- 学习其本地 JSON 与 HF dataset 双入口设计
- 不采用其 README 中旧六任务数学协议

### 13.4 diablo

- 学习其 prompt/response 分开 tokenize 思路
- 学习其 `### Response: Let's think step by step.` 评测 prompt 风格
- 不采用其 `decode` 全串再切 `### Response:` 的评测写法

## 14. 后续实验对齐说明

后续 DSS 数学推理脚本必须全部遵守以下统一规范，不再反复更改：

### 14.1 数据处理脚本

- `math_10k.json` 必须先预处理并 tokenize
- 训练数据保存到 `/root/datasets/math_reasoning/processed/`
- 训练 metadata 保存到 `/root/datasets/math_reasoning/metadata/`
- 评测集不再做二次预处理
- 评测集直接使用 LLM-Adapters 的 `/root/datasets/evaluate/<task>/test.json`

### 14.2 训练脚本

- 统一读取 `processed/` 中的训练数据
- 统一使用 `diablo` 风格 instruction 模板
- 训练 response 保留 `math_10k.json` 原始 `output`
- 不额外追加 `The final answer is: ...`
- 不单独重写 AQuA 输出格式
- 统一 `epochs=3`
- 统一 `max_length=512`
- 统一 `max_prompt_length=256`
- validation 固定切 `500`

### 14.3 评测脚本

- 统一从 `/root/datasets/evaluate/` 读取 LLM-Adapters/MISA-compatible `test.json`
- 统一只 decode 新生成 token
- 数值题统一用最后一个数字
- AQuA 统一取最后一个选项字母
- decode 参数统一为 deterministic 主实验设置
- 每个任务都输出逐样本 json 和总体准确率

### 14.4 日志与结果

- 每个任务终端只打印前 `10` 条样本
- 所有样本结果写入结果 json
- 汇总文件统一记录各任务 accuracy 和平均分

### 14.5 当前已拍板的默认值

```text
train data: math_10k.json
validation_samples: 500
seed: 42
epochs: 3
max_length: 512
max_prompt_length: 256
decode max_new_tokens: 512
decode do_sample: False
decode num_beams: 1
decode temperature: 0.0
DSS target_modules: qkvud
DSS score_method: snr
DSS n_frequency: 180000
DSS candidate_size: 30000
DSS grad_store_steps: 3
DSS batch/global batch: 8 x 2 x 2 = 32
DSS eval_steps/save_steps: 500 / 500
DSS lr search: {5e-5, 1e-4, 1.5e-4, 2e-4}
DSS start lr: 1e-4
```

除非后续专门做 ablation 或发现明确 bug，否则上述默认值不再频繁变动。

## 15. 最终推荐写法

英文版：

```text
For arithmetic reasoning, we follow the corrected Math10K protocol used in recent PEFT works.
We fine-tune LLaMA3-8B and Qwen2.5-7B on math_10k.json for 3 epochs and evaluate on GSM8K, SVAMP, AQuA, and MAWPS.
We exclude AddSub, MultiArith, and SingleEq to avoid MAWPS-derived overlap in the original Math10K setting.
All methods, including LoRA, DoRA, SMT, and DSS, are trained under the same data, prompt, decoding, and answer-extraction pipeline.
```

中文版：

```text
我们采用修正版 Math10K 协议，在 math_10k.json 上训练 LLaMA3-8B 和 Qwen2.5-7B，训练 3 个 epoch，并在 GSM8K、SVAMP、AQuA、MAWPS 上评测。为了避免原始 Math10K 中 MAWPS 来源样本与 AddSub、MultiArith、SingleEq 的重叠问题，我们不报告这三个子任务。所有方法使用相同的数据、prompt、解码和答案抽取脚本。
```

## 16. 当前执行结论

当前数学推理主实验建议直接采用以下协议：

- 模型：`LLaMA3-8B`、`Qwen2.5-7B`
- 训练集：`math_10k.json`
- 评测集：`GSM8K`、`SVAMP`、`AQuA`、`MAWPS`
- 基线：`LoRA`、`DoRA`、`SMT`、`DSS`
- 训练轮数：`3 epochs`
- 验证集：从 `math_10k` 固定切 `500`
- 最大长度：`max_length=512`，`max_prompt_length=256`
- 学习率：DSS 使用 `{5e-5, 1e-4, 1.5e-4, 2e-4}` 搜索，默认第一条 `1e-4`
- DSS 默认：`TARGET_MODULES=qkvud`，`SCORE_METHOD=snr`，`N_FREQUENCY=180000`，`CANDIDATE_SIZE=30000`
- batch 默认：双卡 DDP，`BATCH_SIZE=8`，`GRAD_ACCUM_STEPS=2`，global batch 为 `32`
- checkpoint 默认：`EVAL_STEPS=500`，`SAVE_STEPS=500`
- 训练模板：参考 `diablo`
- 训练 response：保留 `math_10k.json` 原始 `output`，不额外追加答案句
- 评测数据：直接读取 LLM-Adapters 的 `/root/datasets/evaluate`
- decode：`max_new_tokens=512`、`do_sample=False`、`num_beams=1`、`temperature=0.0`
- 输出解析：数值题抽最后一个数值，AQuA 抽最后一个选项字母
- 结果保存：逐样本 json + 终端前 10 条 debug 输出

后续实现应全部围绕这份协议推进，避免在数据、prompt、解码或答案抽取环节引入额外不公平因素。

## 17. 参考

- MISA: Memory-Efficient LLMs Optimization with Module-wise Importance Sampling
  - `https://arxiv.org/pdf/2511.00056`
- LLM-Adapters README
  - `https://github.com/AGI-Edgerunners/LLM-Adapters/blob/main/README.md`
- SMT: Fine-Tuning Large Language Models with Sparse Matrices
  - `https://openreview.net/forum?id=GbgCRJedQ7`
- DiaBlo: Diagonal Blocks Are Sufficient For Fine-tuning
  - `/root/code/diablo`
