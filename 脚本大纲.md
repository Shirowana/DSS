# 脚本大纲





# 新 DSS v0 脚本大纲（以 `diagonalization.py` 复用为核心）

## Summary
- v0 的主线不再围绕 PiCa 展开，shared basis 的实现直接以 [E:\code\new\diagonalization.py](E:\code\new\diagonalization.py) 为原型，尽量“原样复用”它的目标函数、mask、优化流程与返回接口。
- 工程壳子按 `LoCA` 的 PEFT tuner 结构来写，稀疏槽位与候选探测语义借 `SpIEL/SFT`，candidate/elite 语义借旧 `DSS`，但不搬旧 DSS 的重状态机。
- 文件名统一为 `utils.py`，不再使用 `selection_utils.py`。
- 当前 `new/dss/*.py` 还是空文件，这一轮先把“每个脚本必须写什么、哪些接口需要暴露、后续怎样正确注入 PEFT”钉死。

## Script Outline
### `dss/__init__.py`
- 只放导出与注册，不放算法逻辑。
- 导出：
  - `DSSConfig`
  - `DSSLayer`
  - `DSSLinear`
  - `DSSModel`
- 调用：
  - `register_peft_method(name="dss", model_cls=DSSModel, config_cls=DSSConfig)`
- 目标是和现有 `peft.tuners.dss.__init__` 风格一致，保证后续迁入 `peft/src/peft/tuners/dss/` 后可以被标准 PEFT 发现。

### `dss/config.py`
- 只定义配置类 `DSSConfig(PeftConfig)` 和校验逻辑。
- 保留标准 PEFT 字段：
  - `target_modules`
  - `exclude_modules`
  - `layers_to_transform`
  - `layers_pattern`
  - `modules_to_save`
  - `fan_in_fan_out`
  - `bias`
- 新 DSS v0 必须有的字段：
  - `shared_basis_path`: basis 结果文件路径，训练时从这里加载冻结 basis。
  - `basis_group_map`: 功能组定义；默认支持 `q_proj / k_proj / v_proj / up_proj / down_proj`。
  - `elite_size`: 每层真正可训练的 elite 槽位数。
  - `candidate_size`: 每层 probe-only 候选槽位数。
  - `probe_interval`: 每隔多少步做一次 candidate 评分/晋升。
  - `init_candidate_mode`: 候选初始化方式，v0 默认 `random_unique`。
  - `promote_k`: 每次 stage1 最多晋升多少个 candidate 到 elite。
  - `score_metric`: v0 默认基于 core-space gradient magnitude。
  - `score_ema_decay`: 候选分数的 EMA 衰减。
  - `stage2_enabled`: 是否启用第二阶段 prune/grow，v0 默认可以关。
  - `prune_every`
  - `grow_every`
  - `prune_k`
  - `grow_k`
  - `grow_margin`
  - `allow_dense_probe`: v0 默认 `True`，表示允许用 dense core-grad 做 probe。
- `__post_init__` 里只做：
  - `self.peft_type = PeftType.DSS`
  - list -> set 转换
  - 标准 `target_modules` / `layers_pattern` 约束检查
  - elite/candidate/interval 等超参的合法性检查
- 不在 `config.py` 里放：
  - basis 加载
  - 模块分组推断
  - 任何运行时状态

### `dss/shared_basis.py`
- v0 认为这是必需文件。
- 目标：把 shared basis 的离线拟合、加载、变换工具从 `layer.py` 彻底剥离。
- 这里的核心要求是“最大限度复用 `diagonalization.py` 的现有实现”，不重新发明目标函数。
- 必须包含：
  - 一个与现有脚本高度一致的 `diagonalization(...)` 实现。
    - 签名保持兼容：`weights, offset, lr, max_iters, init_A, init_B`
    - loss 仍是 outside-band / inside-band energy ratio
    - 仍返回 `A, B`
  - `fit_shared_basis_for_group(weights, ...)`
    - 薄封装，用于同功能组批量拟合
    - 在返回时额外计算并保存 `A_inv, B_inv`
  - `SharedBasisEntry`
    - 字段：`group_name, A, B, A_inv, B_inv, shape, offset`
  - `SharedBasisPack`
    - 管理 `group -> SharedBasisEntry`
    - 提供 `load(path)` / `validate(group, shape)` / `get(group)`
  - 变换函数：
    - `to_core_space(weight, A, B)`，即 `A @ W @ B`
    - `from_core_space(core, A_inv, B_inv)`，即 `A_inv @ core @ B_inv`
    - `dense_core_grad_from_weight_grad(grad_w, A_inv, B_inv)` 的配套变换工具
- v0 默认策略：
  - basis 只离线拟合，不在线训练。
  - 训练阶段只加载并冻结。
- 不在 v0 做：
  - 自动从模型里抽权重并现场拟合
  - 多设备 basis 拟合调度
  - basis 缓存淘汰或增量更新

### `dss/utils.py`
- 这是选择逻辑和索引工具的纯函数层，替代原先计划中的 `selection_utils.py`。
- 必须包含的函数：
  - `sample_unique_indices(shape, k, forbidden=None, device=None)`
  - `flatten_indices(row_col, n_cols)` / `expand_indices(flat, shape)`
  - `deduplicate_indices(indices)`
  - `mask_out_indices(candidate, occupied)`
  - `promote_candidates(candidate_indices, candidate_scores, free_slots, topk, margin)`
  - `prune_elites(elite_scores, prune_k)`
  - `update_score_ema(old_scores, new_scores, decay)`
- stage1 规则在这里定死：
  - 从 candidate score 排序里选 `top-k`
  - 再用 `margin gate`
  - 再裁剪到 `free_slots`
- stage2 规则也先把纯函数接口定好：
  - prune 只看 elite score 排序
  - grow 只从 candidate pool 补，不允许随机直接进 elite
- 不在这里放：
  - `nn.Module`
  - basis 加载
  - PEFT 注入逻辑
  - optimizer state 变更

### `dss/layer.py`
- 只负责单层 adapter 的参数、前向、最小状态管理与 hook 接口。
- 类结构：
  - `class DSSLayer(BaseTunerLayer)`
  - `class DSSLinear(nn.Module, DSSLayer)`
- `DSSLayer` 必须持有的状态：
  - `elite_values`: `nn.ParameterDict`
  - `elite_indices`: buffer dict
  - `elite_count`
  - `candidate_indices`: buffer dict
  - `candidate_scores`: buffer dict
  - `candidate_age`: buffer dict
  - `phase`: `fill` / `steady`
  - `probe_step_counter`
  - `merged_adapters`
  - `_disable_adapters`
  - `basis_group_name`
  - 每个 adapter 对应的 frozen basis 引用或缓存：
    - `A_inv`
    - `B_inv`
    - 必要时也缓存 `A`, `B`
- `update_layer(...)` 负责：
  - 创建固定大小的 elite trainable 参数槽
  - 初始化 elite/candidate 索引
  - 注册 basis group 与 shape
  - 初始化分数缓冲区
- `get_delta_weight(adapter)` 负责：
  - 将 elite 的 sparse `ΔΛ` 槽位散成 dense core matrix
  - 再用 frozen basis 做 `ΔW = A_inv @ ΔΛ @ B_inv`
- `forward(...)` v0 只支持到：
  - candidate 不参与前向
  - elite 参与前向
  - `result = base_layer(x) + F.linear(x, delta_w)`
  - 或直接 `F.linear(x, base_weight + delta_w, bias)`，二选一但全文件保持一致
- probe 机制：
  - v0 采用 `dense core-grad probe`
  - 即在训练时构造 dense core matrix，让反向图自然产生 core-space gradient
  - 然后只在 `candidate_indices` 上 gather 候选分数
- `layer.py` 只内置最小的 stage1 驱动：
  - 每到 `probe_interval`，调用 `utils.promote_candidates`
  - elite 填满后把 `phase` 切到 `steady`
- stage2 在 `layer.py` 里只保留入口：
  - `maybe_run_stage2()`
  - 默认可以 `TODO` 或默认关闭
- `merge()` / `unmerge()`：
  - v0 只对 elite delta 生效
  - 不碰 candidate
  - 不做复杂导出逻辑

### `dss/model.py`
- 完全按 `LoCA model.py` 的壳子来写。
- 必须实现：
  - `class DSSModel(BaseTuner)`
  - `prefix = "dss_"`
  - `_check_new_adapter_config`
  - `_check_target_module_exists`
  - `_prepare_adapter_config`
  - `_create_and_replace`
  - `_create_new_module`
  - `_replace_module`
  - `_mark_only_adapters_as_trainable`
  - `set_adapter`
  - `enable_adapter_layers`
  - `disable_adapter_layers`
  - `merge_and_unload`
  - `unload`
- `model.py` 的新增职责：
  - 在替换模块时解析当前模块属于哪个 basis group
  - 从 `SharedBasisPack` 里取出该 group 的 frozen basis
  - 把该 basis 交给对应 `DSSLinear`
- v0 的模块支持范围：
  - 主支持 `torch.nn.Linear`
  - `Conv1D` 分支可以保留 PEFT 标准兼容壳子，但不作为首批验证重点
- `model.py` 不负责：
  - basis 拟合
  - candidate 评分规则
  - stage2 复杂调度
  - optimizer state surgery

## PEFT Injection Notes
- 这 6 个文件会按 `peft.tuners.<method>` 风格来写，因此后续迁入 `peft/src/peft/tuners/dss/` 时能直接工作。
- 正确注入所依赖的外部前提：
  - `PeftType.DSS` 已存在。
  - `register_peft_method(name="dss", ...)` 可用。
  - 若训练脚本显式传 `target_modules`，则 v0 不强依赖新增常量映射。
  - 若想支持 `target_modules=None` 的自动推断，则后续需要检查或更新 `TRANSFORMERS_MODELS_TO_DSS_TARGET_MODULES_MAPPING`。
- v0 不引入自定义 trainer，因此标准 `get_peft_model()` 路线应保持可用。

## Test Plan
- 配置层：
  - `DSSConfig` 的字段校验、target/layer pattern 约束正确。
- basis 层：
  - `shared_basis.py` 能正确加载 group basis。
  - shape 不匹配时会报错。
  - `to_core_space` / `from_core_space` 互逆性基本成立。
- layer 层：
  - elite sparse slots 能正确散成 dense `ΔΛ`。
  - `ΔW = A_inv @ ΔΛ @ B_inv` 数值形状正确。
  - candidate 不参与 forward，但能收到 probe score。
  - promote 后 elite/candidate 无重复索引。
- model 层：
  - 目标模块能被正确替换为 `DSSLinear`。
  - 非 adapter 参数被冻结。
  - adapter 开关、merge/unmerge、unload 路径可走通。

## Assumptions
- `shared_basis.py` 中的 `diagonalization(...)` 会尽量保持和现有 [E:\code\new\diagonalization.py](E:\code\new\diagonalization.py) 一致，而不是另起一套实现。
- v0 先追求结构正确与注入正确，不追求最省显存的 probe 实现。
- v0 默认只把 `q_proj / k_proj / v_proj / up_proj / down_proj` 作为 shared-basis group 主路径。
- `utils.py` 是纯函数工具层，不会演变成 `selector.py` 或 `trainer.py`。
- stage2 prune/grow 的完整调度与 optimizer state 更新留到后续版本外提。